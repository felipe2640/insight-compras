EVALUATE
ROW(
    "Linhas", COUNTROWS(ITEMSPEDIDO),
    "NaoVazias", COUNTROWS(
        FILTER(ITEMSPEDIDO, LEN(TRIM(COALESCE(ITEMSPEDIDO[AREFERENCIA2], ""))) > 0)
    ),
    "NulasOuVazias", COUNTROWS(
        FILTER(ITEMSPEDIDO, LEN(TRIM(COALESCE(ITEMSPEDIDO[AREFERENCIA2], ""))) = 0)
    )
)
